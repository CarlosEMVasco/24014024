#include <stdio.h>

int cameraData[10];  // Declaração do vetor com 10 elementos

int main() {
    // Atribuindo valores ao vetor
    for (int i = 0; i < 10; i++) {
        cameraData[i] = i * 10;  // Preenchendo o vetor com múltiplos de 10
    }

    // Imprimindo os valores armazenados no vetor
    printf("Valores no vetor cameraData:\n");
    for (int i = 0; i < 10; i++) {
        printf("cameraData[%d] = %d\n", i, cameraData[i]);
    }

    return 0;
}
